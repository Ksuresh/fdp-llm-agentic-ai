# ============================================================
#  FDP — LLMs & Agentic AI  |  Python Dependencies
#  Install with:  pip install -r requirements.txt
#  Python 3.11+ recommended
# ============================================================

# ── Core ────────────────────────────────────────────────────
python-dotenv>=1.0.0
jupyter>=1.0.0
jupyterlab>=4.0.0
ipykernel>=6.0.0

# ── LLM Providers (Days 1–5) ────────────────────────────────
openai>=1.30.0
anthropic>=0.30.0
google-generativeai>=0.7.0

# ── Open-source LLMs ────────────────────────────────────────
# Ollama runs as a local server — install from https://ollama.com
# No pip package needed for Ollama itself
ollama>=0.2.0

# ── Embeddings & Vector Stores (Day 1 — RAG) ────────────────
faiss-cpu>=1.8.0
chromadb>=0.5.0
tiktoken>=0.7.0

# ── LangChain ecosystem (Days 1, 4) ─────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.2.0
langgraph>=0.1.0
langsmith>=0.1.0

# ── HuggingFace & Fine-tuning (Day 2) ───────────────────────
transformers>=4.40.0
datasets>=2.18.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.27.0
bitsandbytes>=0.43.0        # For QLoRA quantisation
huggingface_hub>=0.22.0

# ── Evaluation (Day 2) ──────────────────────────────────────
ragas>=0.1.0

# ── OpenAI Agents SDK (Day 3) ───────────────────────────────
openai-agents>=0.0.5

# ── CrewAI (Day 3) ──────────────────────────────────────────
crewai>=0.60.0
crewai-tools>=0.8.0

# ── AutoGen (Day 4) ─────────────────────────────────────────
pyautogen>=0.2.0

# ── MCP (Day 5) ─────────────────────────────────────────────
mcp>=1.0.0

# ── Utilities ───────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
pypdf>=4.0.0               # PDF reading for RAG
python-pptx>=0.6.21
Pillow>=10.0.0
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.8.0
tqdm>=4.66.0
rich>=13.0.0               # Pretty terminal output
gradio>=4.0.0              # Quick UI for demos
scikit-learn>=1.5.0

# ── Deployment (Day 4) ──────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0
